{
  "name": "zz-lang",
  "version": "0.1.0",
  "description": "A strongly-typed programming language that compiles to JavaScript",
  "main": "dist/index.js",
  "bin": {
    "zz": "dist/index.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:std": "for f in std/*.zz; do node dist/index.js \"$f\" --stdout > \"std/$(basename \"$f\" .zz).js\"; done",
    "build:all": "npm run build && npm run build:std",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "keywords": [
    "programming-language",
    "compiler",
    "typescript"
  ],
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}